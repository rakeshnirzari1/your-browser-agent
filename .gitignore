# Logs
*.log
npm-debug.log*

# OS junk
.DS_Store
Thumbs.db

# Node (if you ever install dependencies locally)
node_modules/

# Local relay state (if added later)
extension/server/.yba-*

# extension/server/dist/mcp-server.mjs is the shipped, pre-built bundle —
# intentionally NOT ignored; nothing else generates a dist/ folder anymore.
